<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 允许对特定 IP 地址使用自签名证书 -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">39.105.12.124</domain>
        <domain includeSubdomains="false">160.202.238.29</domain>
        <trust-anchors>
            <!-- 信任系统证书 -->
            <certificates src="system" />
            <!-- 信任用户安装的证书（调试用） -->
            <certificates src="user" />
        </trust-anchors>
    </domain-config>

    <!-- 默认配置 -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
