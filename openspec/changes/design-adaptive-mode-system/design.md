# 自适应模式系统 - 详细设计

> **变更ID**: design-adaptive-mode-system
> **日期**: 2026-01-10

## 1. 系统架构总览

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              表现层 (Presentation)                           │
├───────────────────────┬───────────────────────┬─────────────────────────────┤
│      傻瓜模式          │      普通模式          │        高级模式              │
│    Ultra-Simple       │       Simple          │       Advanced             │
│    (IQ 60-80)         │     (IQ 90-110)       │       (IQ 120+)            │
├───────────────────────┴───────────────────────┴─────────────────────────────┤
│                              自适应引擎层                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐        │
│  │ 用户能力分析 │  │ 功能解锁引擎 │  │ 界面复杂度  │  │ 冷启动服务  │        │
│  │  Analyzer   │  │   Unlocker  │  │  Adjuster   │  │ ColdStart   │        │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘        │
├─────────────────────────────────────────────────────────────────────────────┤
│                              业务逻辑层 (Domain)                             │
│         记账服务 | 预算服务 | 统计服务 | 小金库服务 | AI服务                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                              数据层 (Data)                                   │
│                    本地数据库 | 云端同步 | 缓存                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 核心设计原则

| 原则 | 描述 | 实现方式 |
|-----|------|---------|
| **界面与逻辑分离** | 不同模式共享同一套业务逻辑 | 仅UI层有差异 |
| **数据一致性** | 模式切换不影响数据 | 单一数据源 |
| **渐进式复杂度** | 从简单开始，按需增加 | 功能解锁机制 |
| **AI增强简单模式** | 越简单的界面需要越强的AI | 智能填充、语音优先 |

---

## 2. 三种模式详细设计

### 2.1 傻瓜模式 (Ultra-Simple Mode)

#### 2.1.1 设计目标

让 IQ 60-80 的用户也能独立完成记账，无需他人帮助。

#### 2.1.2 核心特征

| 特征 | 规格 | 说明 |
|-----|------|-----|
| 字体 | 1.5倍放大 | 金额48sp，按钮22sp |
| 按钮 | 80dp高度 | 最小触控64dp |
| 页面深度 | 最多3层 | 首页→功能页→完成页 |
| 入口数量 | 最多4个 | 一屏可见 |
| 分类数量 | 8个 | 固定，不可自定义 |
| 语音 | 全程语音反馈 | TTS播报所有操作 |

#### 2.1.3 页面结构

```
傻瓜模式页面树
├── 首页 (SimpleModeHomePage)
│   ├── 余额展示卡片
│   └── 4个大按钮网格
│       ├── 记一笔 → 记账流程
│       ├── 看看账 → 看账页
│       ├── 存钱罐 → 存钱罐页
│       └── 我的 → 我的页
│
├── 记账流程 (3步完成)
│   ├── 步骤1: 金额输入页 (SimpleAmountPage)
│   │   ├── 大标题 "花了多少钱？"
│   │   ├── 金额显示区 (48sp)
│   │   ├── 大数字键盘 (64dp按钮)
│   │   └── 下一步按钮 (80dp)
│   │
│   ├── 步骤2: 分类选择页 (SimpleCategoryPage)
│   │   ├── 金额显示 "花了 ¥35"
│   │   ├── 8个大分类按钮 (2x4网格)
│   │   │   ├── 🍜 吃饭
│   │   │   ├── 🛒 买东西
│   │   │   ├── 🚗 出行
│   │   │   ├── 🏠 住房
│   │   │   ├── 🎮 玩乐
│   │   │   ├── 💊 看病
│   │   │   ├── 📱 话费
│   │   │   └── ··· 其他
│   │   └── 提示 "点一个就行，选错了能改"
│   │
│   └── 步骤3: 成功页 (SimpleSuccessPage)
│       ├── 大对勾动画 ✓
│       ├── 成功文案 "记好了！花了35块钱吃饭"
│       ├── [再记一笔] 主按钮
│       ├── [回到首页] 次要按钮
│       └── 撤销入口 "记错了？点这里改"
│
├── 看账页 (SimpleStatsPage)
│   ├── 月度汇总卡片
│   │   ├── "这个月"
│   │   ├── "花了 ¥2,180"
│   │   └── "还能花 ¥820"
│   ├── 分类消费列表 (带进度条)
│   └── [看全部账单] 按钮
│
├── 账单列表页 (SimpleTransactionListPage)
│   ├── 日期分组
│   ├── 大字号账单卡片
│   │   ├── 分类图标 + 名称
│   │   ├── 金额
│   │   └── 时间
│   └── 滑动删除 (需二次确认)
│
├── 存钱罐页 (SimplePiggyPage)
│   ├── 大猪猪图标 🐷
│   ├── 存款金额 (48sp)
│   ├── 目标进度条
│   ├── [存钱进去] 主按钮
│   └── [取钱出来] 次要按钮
│
├── 我的页 (SimpleProfilePage)
│   ├── 头像和昵称
│   ├── 菜单列表
│   │   ├── 📊 看看报表
│   │   ├── ☁️ 存到云上
│   │   ├── ⚙️ 设置
│   │   └── ❓ 帮助
│   └── [退出简单模式] 切换入口
│
└── 帮助页 (SimpleHelpPage)
    ├── 语音指引按钮
    │   ├── "怎么记账？" → 语音播放
    │   ├── "怎么看账？" → 语音播放
    │   └── "怎么存钱？" → 语音播放
    └── 联系帮助入口
```

#### 2.1.4 傻瓜模式UI规范

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              傻瓜模式视觉规范                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【字体规范】                                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │  用途           标准模式     傻瓜模式                              │       │
│  │  ─────────────────────────────────────────                        │       │
│  │  超大金额        32sp        48sp  (Bold)                         │       │
│  │  大标题          24sp        32sp  (Bold)                         │       │
│  │  标题            18sp        26sp  (Medium)                       │       │
│  │  按钮文字        16sp        22sp  (Medium)                       │       │
│  │  正文            14sp        18sp  (Regular)                      │       │
│  │  说明文字        12sp        16sp  (Regular)                      │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                             │
│  【尺寸规范】                                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │  主按钮高度：     80dp                                            │       │
│  │  最小触控区域：   64dp × 64dp                                     │       │
│  │  按钮圆角：       16dp                                            │       │
│  │  按钮间距：       16dp                                            │       │
│  │  页面内边距：     20dp                                            │       │
│  │  图标尺寸：       32-40dp                                         │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                             │
│  【颜色规范】                                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │  主色调：   #2196F3 (蓝)    - 主要操作                            │       │
│  │  成功色：   #4CAF50 (绿)    - 成功状态                            │       │
│  │  警告色：   #FF9800 (橙)    - 警告提示                            │       │
│  │  危险色：   #F44336 (红)    - 危险操作                            │       │
│  │  背景色：   #F5F5F5 (浅灰)  - 页面背景                            │       │
│  │  卡片色：   #FFFFFF (白)    - 卡片背景                            │       │
│  │  主文字：   #212121 (深灰)  - 主要文字                            │       │
│  │  次文字：   #757575 (中灰)  - 次要文字                            │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                             │
│  【对比度要求】                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐       │
│  │  文字对比度 ≥ 7:1 (高于标准 4.5:1)                                 │       │
│  │  重要按钮使用高饱和度颜色                                          │       │
│  └─────────────────────────────────────────────────────────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 2.1.5 傻瓜模式交互规范

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              傻瓜模式交互规范                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  【反馈三要素】                                                               │
│                                                                             │
│  1. 视觉反馈                                                                 │
│     ├── 按钮按下：缩小0.98 + 变色                                            │
│     ├── 成功：大对勾 ✓ + 绿色                                                │
│     ├── 失败：大叉号 ✗ + 红色                                                │
│     └── 加载中：转圈动画                                                     │
│                                                                             │
│  2. 触觉反馈 (震动)                                                          │
│     ├── 按钮点击：轻震 10ms                                                  │
│     ├── 操作成功：中震 50ms                                                  │
│     ├── 删除确认：重震 100ms                                                 │
│     └── 错误提示：双震 2×30ms                                                │
│                                                                             │
│  3. 听觉反馈 (语音)                                                          │
│     ├── 所有操作结果语音播报                                                 │
│     ├── 成功："记好了！花了35块钱吃饭"                                        │
│     ├── 失败："没存上，再试一次？"                                            │
│     └── 等待："正在记..."                                                    │
│                                                                             │
│  【防呆设计】                                                                 │
│                                                                             │
│  1. 金额输入                                                                 │
│     ├── 自动过滤非数字字符                                                   │
│     ├── 超大金额(>10000)二次确认："是不是多打了个0？"                          │
│     └── 零金额提示："忘记输金额了"                                            │
│                                                                             │
│  2. 删除操作                                                                 │
│     ├── 第1次确认："要删掉这笔账吗？"                                         │
│     ├── 第2次确认："真的删？删了就没了哦"                                      │
│     └── 删除后10秒内可撤销                                                   │
│                                                                             │
│  3. 误触保护                                                                 │
│     ├── 危险按钮需长按触发                                                   │
│     ├── 危险按钮远离常用按钮                                                 │
│     └── 滑动删除需滑够距离                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 普通模式 (Simple Mode)

#### 2.2.1 设计目标

为普通用户提供完整但不复杂的记账体验，功能按需解锁。

#### 2.2.2 与傻瓜模式的差异

| 维度 | 傻瓜模式 | 普通模式 |
|-----|---------|---------|
| 字体 | 1.5倍 | 1.2倍 |
| 按钮高度 | 80dp | 56dp |
| 分类数量 | 8个固定 | 12-20个可自定义 |
| 页面深度 | 最多3层 | 最多4层 |
| 功能数量 | 4个核心 | 8-12个 |
| 语音反馈 | 全程 | 关键操作 |

#### 2.2.3 可解锁功能清单

| Level | 功能 | 解锁条件 | 描述 |
|-------|-----|---------|------|
| L1 | 查看历史 | 使用3天 | 查看所有历史账单 |
| L1 | 简单统计 | 记录10笔 | 本月花费统计 |
| L2 | 预算设置 | 使用7天 | 设置月度预算 |
| L2 | 分类管理 | 记录50笔 | 自定义分类 |
| L2 | 月度报表 | 使用14天 | 月度收支报表 |
| L3 | 账户管理 | 使用14天 | 多账户管理 |
| L3 | 趋势分析 | 使用30天 | 消费趋势图表 |
| L3 | 目标设置 | 使用30天 | 储蓄目标管理 |

### 2.3 高级模式 (Advanced Mode)

#### 2.3.1 设计目标

为高级用户提供完整功能集，快速解锁所有功能。

#### 2.3.2 特征

- 标准字体和按钮尺寸
- 完整的功能导航
- 复杂的数据可视化
- 高级筛选和搜索
- 批量操作支持

#### 2.3.3 解锁速度

高级用户的解锁条件降为普通用户的50%：
- 使用7天即可解锁L3功能
- 记录25笔即可解锁分类管理
- 可手动一键解锁所有功能

---

## 3. 自适应引擎设计

### 3.1 用户能力分析器

```dart
/// 用户能力分析服务
class UserCapabilityAnalyzer {
  /// 分析指标权重
  static const Map<String, double> weights = {
    'errorRate': 0.25,        // 错误率
    'taskSpeed': 0.20,        // 任务完成速度
    'helpRequests': 0.15,     // 求助次数
    'exploration': 0.15,      // 功能探索率
    'sessionDuration': 0.15,  // 使用时长
    'consecutiveDays': 0.10,  // 连续使用天数
  };

  /// 计算用户能力等级
  UserCapabilityLevel calculateLevel(UserBehaviorData data) {
    double score = 0;

    // 1. 错误率评分 (低错误 = 高分)
    if (data.errorRate < 0.05) score += 25;
    else if (data.errorRate < 0.15) score += 15;
    else if (data.errorRate < 0.30) score += 5;

    // 2. 任务完成速度评分
    if (data.avgTaskTimeSeconds < 20) score += 20;
    else if (data.avgTaskTimeSeconds < 40) score += 12;
    else if (data.avgTaskTimeSeconds < 60) score += 5;

    // 3. 求助次数评分 (少求助 = 高分)
    if (data.helpRequestsPerSession < 0.5) score += 15;
    else if (data.helpRequestsPerSession < 1.0) score += 8;
    else if (data.helpRequestsPerSession < 2.0) score += 3;

    // 4. 功能探索率评分
    if (data.featureExplorationRate > 0.6) score += 15;
    else if (data.featureExplorationRate > 0.3) score += 8;
    else if (data.featureExplorationRate > 0.1) score += 3;

    // 5. 使用时长评分
    if (data.avgSessionMinutes > 5) score += 15;
    else if (data.avgSessionMinutes > 2) score += 8;
    else if (data.avgSessionMinutes > 1) score += 3;

    // 6. 连续使用评分
    if (data.consecutiveDays > 7) score += 10;
    else if (data.consecutiveDays > 3) score += 5;
    else if (data.consecutiveDays > 1) score += 2;

    // 总分100分，分级
    if (score >= 70) return UserCapabilityLevel.advanced;
    if (score >= 40) return UserCapabilityLevel.intermediate;
    return UserCapabilityLevel.basic;
  }
}

enum UserCapabilityLevel {
  basic,        // 基础用户 (傻瓜模式)
  intermediate, // 普通用户 (普通模式)
  advanced,     // 高级用户 (高级模式)
}
```

### 3.2 功能解锁引擎

```dart
/// 功能解锁推荐引擎
class FeatureUnlockEngine {
  /// 获取推荐解锁的功能
  Future<FeatureRecommendation?> getRecommendation(
    UserProfile user,
    AppUsageData usage,
  ) async {
    // 1. 获取所有未解锁功能
    final lockedFeatures = await getLockedFeatures(user);

    // 2. 过滤满足条件的功能
    final eligible = lockedFeatures.where((f) {
      return f.checkUnlockCondition(user, usage);
    }).toList();

    if (eligible.isEmpty) return null;

    // 3. 按推荐分数排序
    eligible.sort((a, b) {
      final scoreA = calculateRecommendationScore(a, user, usage);
      final scoreB = calculateRecommendationScore(b, user, usage);
      return scoreB.compareTo(scoreA);
    });

    // 4. 返回最佳推荐
    return FeatureRecommendation(
      feature: eligible.first,
      reason: generateReason(eligible.first, user, usage),
      confidence: calculateConfidence(eligible.first, user, usage),
    );
  }

  /// 计算推荐分数
  double calculateRecommendationScore(
    Feature feature,
    UserProfile user,
    AppUsageData usage,
  ) {
    double score = 0;

    // 用户能力匹配度 (40%)
    score += getCapabilityMatch(feature, user) * 0.4;

    // 使用场景匹配度 (30%)
    score += getUsageMatch(feature, usage) * 0.3;

    // 功能重要性 (20%)
    score += feature.importance * 0.2;

    // 用户兴趣度 (10%)
    score += getUserInterest(feature, user) * 0.1;

    return score;
  }

  /// 生成推荐理由
  String generateReason(Feature feature, UserProfile user, AppUsageData usage) {
    switch (feature.id) {
      case 'category_management':
        return '你已经记了${usage.transactionCount}笔，可以用分类更好地管理';
      case 'budget_setting':
        return '用了${usage.daysUsed}天了，设置预算可以控制花钱';
      case 'monthly_report':
        return '月末了，看看这个月花了多少钱';
      default:
        return feature.defaultReason;
    }
  }
}
```

### 3.3 自适应节奏控制

```dart
/// 自适应推荐节奏
class AdaptiveUnlockPacing {
  /// 根据用户能力调整推荐间隔
  Duration getRecommendationInterval(UserCapabilityLevel level) {
    switch (level) {
      case UserCapabilityLevel.basic:
        return Duration(days: 14); // 2周推荐一次
      case UserCapabilityLevel.intermediate:
        return Duration(days: 7);  // 1周推荐一次
      case UserCapabilityLevel.advanced:
        return Duration(days: 3);  // 3天推荐一次
    }
  }

  /// 根据用户能力调整解锁条件倍数
  double getConditionMultiplier(UserCapabilityLevel level) {
    switch (level) {
      case UserCapabilityLevel.basic:
        return 1.5;  // 条件更严格，确保准备好了
      case UserCapabilityLevel.intermediate:
        return 1.0;  // 标准条件
      case UserCapabilityLevel.advanced:
        return 0.5;  // 条件放宽，快速解锁
    }
  }
}
```

---

## 4. 冷启动策略

### 4.1 零配置启动

```
第1天：完全零配置
├── 不问任何问题
├── 默认使用傻瓜模式
├── 只需要会点"花钱"按钮
└── AI自动处理分类、账户等

第2-6天：静默学习
├── 用户自由使用
├── 系统收集行为数据
├── 分析消费模式
└── 准备智能建议

第7天：智能配置建议
├── "用了一周了，要设置预算吗？"
├── "根据这周花费，建议每月3000元"
├── 用户一键确认
└── 不确认也不影响使用
```

### 4.2 智能默认值

```dart
/// 智能默认值生成
class SmartDefaultGenerator {
  /// 生成预算建议
  SimpleBudget generateBudgetSuggestion(List<Transaction> transactions) {
    // 计算日均支出
    final days = getDaysSpan(transactions);
    final totalExpense = transactions
        .where((t) => t.type == TransactionType.expense)
        .fold(0.0, (sum, t) => sum + t.amount);

    final dailyAvg = totalExpense / days;

    // 推算月度预算 (日均 × 30 × 1.2缓冲)
    final monthlyEstimate = dailyAvg * 30 * 1.2;

    // 向上取整到百位
    final suggested = (monthlyEstimate / 100).ceil() * 100;

    return SimpleBudget(
      recommended: suggested.toDouble(),
      higher: (suggested * 1.3).roundToDouble(),
      lower: (suggested * 0.7).roundToDouble(),
    );
  }

  /// 智能分类推荐
  String suggestCategory(DateTime time, String? merchant) {
    // 基于时间推荐
    final hour = time.hour;
    if (hour >= 7 && hour < 9) return '早餐';
    if (hour >= 11 && hour < 14) return '午餐';
    if (hour >= 17 && hour < 21) return '晚餐';

    // 基于商户推荐
    if (merchant != null) {
      return matchMerchantCategory(merchant);
    }

    return '其他';
  }
}
```

---

## 5. 模式切换机制

### 5.1 切换流程

```dart
/// 模式切换管理器
class ModeManager {
  final UIModeProvider _modeProvider;
  final FeatureStateRepository _featureRepo;

  /// 切换到目标模式
  Future<void> switchMode(UIMode targetMode) async {
    // 1. 保存当前状态
    await saveCurrentState();

    // 2. 验证切换条件
    if (!canSwitchTo(targetMode)) {
      throw ModeSwitchException('当前无法切换到该模式');
    }

    // 3. 执行切换
    _modeProvider.setMode(targetMode);

    // 4. 调整功能可见性
    await adjustFeatureVisibility(targetMode);

    // 5. 播放切换反馈
    await playModeChangeFeedback(targetMode);
  }

  /// 检查是否可以切换
  bool canSwitchTo(UIMode targetMode) {
    // 任何模式都可以切换到傻瓜模式
    if (targetMode == UIMode.ultraSimple) return true;

    // 傻瓜模式切换到普通模式需要满足条件
    if (_modeProvider.currentMode == UIMode.ultraSimple &&
        targetMode == UIMode.simple) {
      return hasUnlockedBasicFeatures();
    }

    // 普通模式切换到高级模式需要满足条件
    if (_modeProvider.currentMode == UIMode.simple &&
        targetMode == UIMode.advanced) {
      return hasUnlockedAdvancedFeatures();
    }

    return true;
  }
}
```

### 5.2 数据一致性保证

```dart
/// 模式切换时的数据处理
class ModeTransitionHandler {
  /// 切换前保存状态
  Future<void> saveCurrentState() async {
    // 保存当前页面路由
    await saveCurrentRoute();

    // 保存未完成的草稿
    await saveDrafts();

    // 保存用户偏好
    await savePreferences();
  }

  /// 切换后恢复状态
  Future<void> restoreState(UIMode newMode) async {
    // 恢复到对应模式的首页
    navigateToModeHome(newMode);

    // 同步功能状态
    await syncFeatureStates();

    // 刷新数据
    await refreshData();
  }
}
```

---

## 6. 页面路由设计

### 6.1 模式感知路由

```dart
/// 模式感知路由器
class ModeAwareRouter {
  static final Map<UIMode, Map<String, WidgetBuilder>> _pageRegistry = {
    UIMode.ultraSimple: {
      '/': (ctx) => const SimpleModeHomePage(),
      '/add': (ctx) => const SimpleAmountPage(),
      '/add/category': (ctx) => const SimpleCategoryPage(),
      '/add/success': (ctx) => const SimpleSuccessPage(),
      '/stats': (ctx) => const SimpleStatsPage(),
      '/transactions': (ctx) => const SimpleTransactionListPage(),
      '/piggy': (ctx) => const SimplePiggyPage(),
      '/profile': (ctx) => const SimpleProfilePage(),
      '/help': (ctx) => const SimpleHelpPage(),
    },
    UIMode.simple: {
      '/': (ctx) => const NormalHomePage(),
      '/add': (ctx) => const NormalAddPage(),
      '/stats': (ctx) => const NormalStatsPage(),
      '/transactions': (ctx) => const NormalTransactionListPage(),
      '/budget': (ctx) => const NormalBudgetPage(),
      '/categories': (ctx) => const NormalCategoriesPage(),
      '/accounts': (ctx) => const NormalAccountsPage(),
      '/profile': (ctx) => const NormalProfilePage(),
    },
    UIMode.advanced: {
      // 完整功能集
      '/': (ctx) => const AdvancedHomePage(),
      // ... 所有高级页面
    },
  };

  /// 获取当前模式的页面
  Widget getPage(String route, UIMode mode) {
    final modePages = _pageRegistry[mode];
    final builder = modePages?[route];

    if (builder == null) {
      // 回退到上一级模式的页面
      return getFallbackPage(route, mode);
    }

    return builder(context);
  }
}
```

### 6.2 导航守卫

```dart
/// 导航守卫：检查功能是否已解锁
class FeatureGuard {
  final FeatureStateRepository _featureRepo;

  /// 检查目标路由是否可访问
  Future<bool> canNavigate(String route) async {
    final requiredFeature = getRequiredFeature(route);

    if (requiredFeature == null) {
      return true; // 基础功能，无需检查
    }

    return await _featureRepo.isUnlocked(requiredFeature);
  }

  /// 拦截并处理未解锁功能
  Future<NavigationResult> guard(String route) async {
    if (await canNavigate(route)) {
      return NavigationResult.allow;
    }

    // 显示解锁提示
    final shouldUnlock = await showUnlockPrompt(route);

    if (shouldUnlock) {
      await unlockFeature(getRequiredFeature(route)!);
      return NavigationResult.allow;
    }

    return NavigationResult.deny;
  }
}
```

---

## 7. 前端原型设计

### 7.1 原型页面清单

| 模式 | 页面 | 优先级 | 状态 |
|-----|------|-------|------|
| 傻瓜 | 首页 | P0 | 已完成 |
| 傻瓜 | 金额输入 | P0 | 已完成 |
| 傻瓜 | 分类选择 | P0 | 已完成 |
| 傻瓜 | 记账成功 | P0 | 已完成 |
| 傻瓜 | 看账页 | P0 | 已完成 |
| 傻瓜 | 存钱罐 | P0 | 已完成 |
| 傻瓜 | 我的页 | P0 | 已完成 |
| 傻瓜 | 账单列表 | P1 | 待设计 |
| 傻瓜 | 收入记账 | P1 | 待设计 |
| 傻瓜 | 设置页 | P1 | 待设计 |
| 傻瓜 | 帮助页 | P1 | 待设计 |
| 傻瓜 | 报表页 | P2 | 待设计 |
| 傻瓜 | 云同步页 | P2 | 待设计 |
| 普通 | 功能货架 | P1 | 待设计 |
| 普通 | 解锁弹窗 | P1 | 待设计 |
| 普通 | 预算设置 | P2 | 待设计 |
| 普通 | 分类管理 | P2 | 待设计 |

### 7.2 原型文件结构

```
docs/prototype/
├── simple_mode_prototype.html      # 傻瓜模式完整原型 (已有)
├── simple_mode_extended.html       # 傻瓜模式扩展页面 (待创建)
│   ├── 账单列表页
│   ├── 收入记账页
│   ├── 设置页
│   ├── 帮助页
│   └── 报表页
├── normal_mode_prototype.html      # 普通模式原型 (待创建)
│   ├── 功能货架
│   ├── 解锁弹窗
│   └── 扩展功能页面
└── mode_switch_demo.html           # 模式切换演示 (待创建)
```

---

## 8. 技术实现要点

### 8.1 关键组件

```dart
// 1. 简单模式脚手架
class SimpleModeScaffold extends StatelessWidget {
  final Widget child;
  final String? title;
  final bool showBackButton;
  final bool showVoiceButton;
  final VoidCallback? onBack;

  // 统一的简单模式页面框架
}

// 2. 大按钮组件
class MegaButton extends StatelessWidget {
  final String label;
  final String? description;
  final IconData icon;
  final VoidCallback onPressed;

  // 80dp高度，大字体，图标+文字
}

// 3. 语音反馈服务
class VoiceFeedbackService {
  Future<void> speak(String text);
  Future<void> playSuccess();
  Future<void> playError();
}

// 4. 触觉反馈服务
class HapticFeedbackService {
  void lightTap();
  void mediumTap();
  void heavyTap();
  void doubleTap();
}
```

### 8.2 状态管理

```dart
// 使用 Riverpod 管理全局状态
final uiModeProvider = StateNotifierProvider<UIModeNotifier, UIModeState>((ref) {
  return UIModeNotifier();
});

final unlockedFeaturesProvider = StateNotifierProvider<UnlockedFeaturesNotifier, Set<String>>((ref) {
  return UnlockedFeaturesNotifier();
});

final userCapabilityProvider = Provider<UserCapabilityLevel>((ref) {
  final analyzer = ref.watch(capabilityAnalyzerProvider);
  final usage = ref.watch(usageDataProvider);
  return analyzer.calculateLevel(usage);
});
```

---

## 9. 成功标准

### 9.1 用户体验指标

| 指标 | 傻瓜模式 | 普通模式 | 高级模式 |
|-----|---------|---------|---------|
| 首次使用成功率 | ≥95% | ≥90% | ≥85% |
| 任务完成时间 | ≤30秒 | ≤20秒 | ≤15秒 |
| 错误恢复率 | ≥90% | ≥85% | ≥80% |
| 7日留存率 | ≥80% | ≥75% | ≥70% |
| 满意度评分 | ≥4.5/5 | ≥4.0/5 | ≥4.0/5 |

### 9.2 技术指标

| 指标 | 目标 |
|-----|------|
| 模式切换耗时 | <500ms |
| 页面加载时间 | <1s |
| 内存占用增量 | <5MB |
| 崩溃率 | <0.1% |
